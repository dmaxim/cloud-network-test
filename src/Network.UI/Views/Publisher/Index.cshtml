

<h2>My Publisher</h2>
<div class="card">
    <div class="card-title">Publisher</div>
    <div class="card-body">
        <form asp-action="Publish" asp-controller="Publisher">
            <input type="submit" class="btn btn-sm btn-primary" value="Publish">
        </form>
    </div>
</div>